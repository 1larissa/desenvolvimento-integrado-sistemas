//Classe principal que se roda TUDO, 
//se você cria outra classe, crie ela aqui etc

#pragma once /*Sempre coloque em todo .h pra
                não ter problema de include*/
#include <stdio.h>
#include "server.hpp"

class Principal{
    private:
        //server servidor;
        //atributos e outros itens
    public:
        //metodos
        Principal();
        void executar();
};